function scene(settings)
{
    this.objects    = settings.objects
    this.lights     = settings.lights
    this.camera     = settings.camera
    this.bgcolor    = settings.bgcolor || [0, 0, 0]
}